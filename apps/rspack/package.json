{
  "name": "@zod-aot/rspack",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rspack build",
    "start": "node dist/main.js",
    "test": "pnpm build && node dist/main.js"
  },
  "dependencies": {
    "zod": "catalog:",
    "zod-aot": "workspace:*"
  },
  "devDependencies": {
    "@rspack/cli": "^2.1.4",
    "@rspack/core": "^2.1.4",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
